# Holdfast submission record

## Thesis and boundary

Holdfast lets a promisor bond a precise claim against a dated archived web capture. GenLayer is
necessary because archive retrieval and document interpretation are external facts. Deterministic
code owns byte limits, archive-index admission, digest/length gates, deadlines, contests,
settlement and bond accounting. Consensus fetches the cited archive records and, only after the
contract admits the bytes, may interpret the bounded text. Archive availability is not treated as
a breach.

## Repository and deployment status

Live app: https://holdfast-orpin.vercel.app

The fixture corpus is self-contained under `tests/fixtures/holdfast/`; `scripts/verify_fixtures.py`
re-derives its manifest from those bytes. Any address in `DEPLOYMENT.json` is canonical only when
its finalized receipt, schema and byte-for-byte source parity match the final Git commit. The
canonical deployment is `0x0D656F1A319Dad705eeE9CF25045CF22a05776B9`, transaction
`0x19543305c177b30e8fd5308d26256eb24416d6994e1bde659fa68037bb17f07e`; payable live exercises
remain limited and are labelled in the evidence record.

## Verification

```text
npm ci
npm run verify           # includes verify:citations, verify:fixtures, verify:emdash,
                          # verify:splice, verify:gates, verify:deployment, verify:schema
python scripts/verify_fixtures.py
python -m pytest tests/direct -q
python scripts/measure-gates.py --check
```

`npm run verify` now runs deployment-source and schema parity as part of the normal chain, not
as a separate manual step: a source change that was never redeployed, or a redeployment that
drifted from source, fails CI rather than being missed.

The direct state machine, archive failure gates, malformed captures and value branches are
**PROVEN DIRECT**. Bond creation, admission, contest, expiry and settlement are **PROVEN LIVE**
only when listed with finalized transaction identifiers and stored state in the evidence record.
The invariant is `total_bonded = open obligations + settled payouts + refunded payouts` and the
contract has no hidden sweep authority.

## Evidence table

| Capability | Evidence |
|---|---|
| Canonical deployment | PROVEN LIVE |
| Source parity | PROVEN |
| Schema parity | PROVEN |
| Contract reads (`get_bond`, `list_bonds`, `get_ledger`, `bond_history`) | PROVEN LIVE |
| Zero-value `create_bond` refusal | PROVEN LIVE |
| Funded `create_bond` on the canonical deployment | NOT PROVEN |
| Live `check_commitment` on the canonical deployment | NOT PROVEN |
| Archive admission (decompression, digest, byte caps) | PROVEN DIRECT |
| Contest | PROVEN DIRECT |
| Settlement | PROVEN DIRECT |
| Expiry / refund | PROVEN DIRECT |
| Replay-pin protection on contest/settlement re-reads | PROVEN DIRECT (`_require_stable_replay`, exercised by `tests/direct/test_lifecycle.py`) |
| Gate A | DISABLED BY DEFAULT, after one measured false-rejection (an SPA chrome rebuild inflated a page 13x with no policy change); see `GATE_A_ENABLED_DEFAULT` in `contracts/Holdfast.py` and `evidence/gate-measurements.json` |
| Gates B / C / D | 5/5 real captures in this project's fixture set qualify once decoded; **0 measured true positives against a real deficient archive capture** (none has been captured); 1/1 synthetic negative (`snap-github-tos-chrome-only.bin`, labelled as synthetic) correctly rejected. Retained as a fail-closed structural safeguard, not as data-backed discrimination against a real bad document. Full per-fixture measurement in `evidence/gate-measurements.json`, regenerated by `python scripts/measure-gates.py` and checked in CI by `npm run verify:gates`. |
| `renew_bond` | NOT PRESENT. No such method exists in `contracts/Holdfast.py`; an external review claim referencing renewal is stale. The contract documents the omission directly, next to `expire_bond`. |

A **PROVEN DIRECT** row means the behavior is exercised through the public contract methods
under the real GenVM SDK (`tests/direct/`), not mocked. It is not a live StudioNet transaction.
Nothing here is described as live proof unless a finalized transaction hash backs it.

## Reviewer walkthrough

1. Run the fixture verifier and inspect the manifest/byte checks.
2. Read `contracts/Holdfast.py` around the strict archive blocks and settlement paths.
3. Run frontend and Direct Mode suites from a clean clone.
4. Run deployment, schema and evidence verification; retain **NOT PROVEN** labels for any branch
   requiring a naturally occurring real-world breach.
